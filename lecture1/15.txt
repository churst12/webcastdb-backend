by doing thisso this is the Box Trace so box next box is 2 smaller rise of n + - 2 and -2 -2 and -4 those would be the size of the boxes and then at the bottom level you're either have an empty array or an array with just one element in it I mean both those items at those both those have to be a possibility because if the original array had an even number of elements you'll halt when you reach an empty array if the original array had an odd number of elements you'll halt when you